type FormState = {
  id: number;
  name: string;
  surname: string;
  email: string;
  dateOfBirth: string;
  selectGender: string;
  subscribe: boolean;
  termsAndConditions: boolean;
};

export default FormState;
